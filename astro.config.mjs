import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://Tew0979271981.github.io', // **ต้องเป็นชื่อผู้ใช้ GitHub ของคุณ**
  base: '/ProLang', // **ต้องเป็นชื่อ Repository ของคุณ**
});